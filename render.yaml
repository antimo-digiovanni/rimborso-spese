services:
  - type: web
    name: expense-hub
    env: python
    buildCommand: pip install -r requirements.txt
    preDeployCommand: python manage.py migrate --noinput
    startCommand: python -m gunicorn expense_hub_project.wsgi:application --bind 0.0.0.0:${PORT:-10000}
    autoDeploy: true